<div class="table-container">
<div class='breadcrumb'>
<a id="show-users" >All The Things</a>
  <span class="glyphicon glyphicon-chevron-right"></span>
 <a id="user-folders" data-folder="{{folder}}" data-id="{{uploads.0._owner}}">{{folder}}</a>
 <span class="glyphicon glyphicon-chevron-right"></span>
 <span id="current-folder">{{uploads.0.path}}</span>
</div>

<table class="table table-responsive" style="float:right">
<thead class="thead-default">
  <tr>
    <th style="text-align:center">Preview</th>
    <th style="padding-left: 15px;">Name</th>
    <th>Created</th>
    <th>Updated</th>
    <th style="text-align:center" >Actions</th>
  </tr>
  </thead>
  <tbody>
    {{#each uploads as |upload|}}
    <tr style="height: 115px;" class="clickable">
      <td style="text-align:center" data-url="{{upload.url}}" class="file file-path clickable">
        {{#if upload.isImage}}
        <img src="{{upload.url}}" class="img-thumbnail" alt="{{upload.title}}"  >
        {{else}}
      <img style="padding-bottom: 15px;" src="../../../assets/images/all-the-things-template.jpg.png"
      class="img-thumbnail" alt="{{upload.title}}">
        {{/if}}
      </td>

      <td data-url="{{upload.url}}" class="file-path clickable">
      <span>{{upload.title}}</span>
      </td>

    <td data-url="{{upload.url}}" class="file-path date clickable">{{upload.createdAt}}</td>

    <td data-url="{{upload.url}}" class="file-path date clickable">{{upload.updatedAt}}</td>

        <td style="text-align:right;" class="file">
            <a href="{{upload.url}}" download class="btn btn-default" aria-hidden="true">
              <span class="glyphicon glyphicon-download"></span>
            </a>
        {{#if upload.editable}}
        <button type="button" class="btn btn-default update-button" data-id="{{upload.id}}">
          <span class="glyphicon glyphicon-pencil" aria-hidden="true"></span>
        </button>
        <button type="button" class="btn btn-default delete-button" data-id="{{upload.id}}">
          <span class="glyphicon glyphicon-trash" aria-hidden="true"></span>
        </button>
      </td>
      {{/if}}
    </tr>
    {{/each}}
  </tbody>
</table>
</div>
